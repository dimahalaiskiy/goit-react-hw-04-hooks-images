import styled from 'styled-components';

const ImageList = styled.ul`
	padding: 0;
	margin: 0;
	padding: 15px;
	display: flex;
	flex-wrap: wrap;
	justify-content: center;
	margin-bottom: -10px;
`;

export { ImageList };
